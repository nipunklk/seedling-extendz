import { BrandDirective } from './brand.directive';

describe('BrandDirective', () => {
  it('should create an instance', () => {
    const directive = new BrandDirective();
    expect(directive).toBeTruthy();
  });
});
