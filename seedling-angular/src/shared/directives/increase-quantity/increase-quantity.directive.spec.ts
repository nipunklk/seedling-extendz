import { IncreaseQuantityDirective } from './increase-quantity.directive';

describe('IncreaseQuantityDirective', () => {
  it('should create an instance', () => {
    const directive = new IncreaseQuantityDirective();
    expect(directive).toBeTruthy();
  });
});
