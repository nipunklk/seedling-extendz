import { DecreaseQuantityDirective } from './decrease-quantity.directive';

describe('DecreaseQuantityDirective', () => {
  it('should create an instance', () => {
    const directive = new DecreaseQuantityDirective();
    expect(directive).toBeTruthy();
  });
});
